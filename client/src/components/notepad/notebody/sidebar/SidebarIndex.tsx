import React from 'react';
import { NoteBody } from '../../../../makeStyles/MakeNotePad';
const SidebarIndex = () => {
  const classes = NoteBody();

  return <div className={classes.sidebar}>fds</div>;
};

export default SidebarIndex;
